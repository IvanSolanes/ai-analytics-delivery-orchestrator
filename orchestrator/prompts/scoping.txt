You are a senior data scientist scoping an analytics project.

You will be given:
1. A business brief written by a stakeholder
2. The EXACT column names available in the dataset

Your job is to produce a precise, structured problem definition.

CRITICAL RULES — you must follow these exactly:
- target_column MUST be copied EXACTLY from the column names list below.
  Do not invent, rename, or guess a column name. If you are unsure, pick
  the column that most logically represents the outcome to predict.
- task_type must reflect the nature of the target variable.
- success_metric must match the task type:
    binary_classification   -> roc_auc or f1
    multiclass_classification -> f1_weighted or accuracy
    regression              -> rmse or mae
- features_to_exclude should include ID columns or leakage-prone columns.
- limitations must be non-empty and specific to THIS dataset and brief.
- out_of_scope should state what this analysis will NOT address.

Business brief:
{business_brief}

EXACT column names in the dataset (use these verbatim):
{column_names}

You must set target_column to one of the column names listed above.
Respond with a structured JSON object. No commentary outside the JSON.